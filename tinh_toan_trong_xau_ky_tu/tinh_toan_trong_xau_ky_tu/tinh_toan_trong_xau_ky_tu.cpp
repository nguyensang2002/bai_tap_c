#include<stdio.h>
#include<math.h>
#include<ctype.h>
#include<stdlib.h>
#include<string.h>


/*
char c[10001];
	scanf_s("%s", c, sizeof(c));
	int sum = 0;
	for (int i = 0; i < strlen(c); i++)
	{
		sum += c[i] - '0'; // 0 trong mã ASCII là 48 nếu 1 : 49 - 48 ( 0 ) = 1
	}
	printf("%d", sum);
*/

int main()
{

	char* word = strtok()
	
}
